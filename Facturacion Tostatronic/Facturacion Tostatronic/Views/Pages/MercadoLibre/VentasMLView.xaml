<UserControl x:Class="Facturacion_Tostatronic.Views.Pages.MercadoLibre.VentasMLView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Facturacion_Tostatronic.Views.Pages.MercadoLibre"
      xmlns:vm="clr-namespace:Facturacion_Tostatronic.ViewModels.MercadoLibre"
      xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:MercadoLibreViewModel />
    </UserControl.DataContext>
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="10">
            <TextBlock VerticalAlignment="Center" Text="Buscar por ID de venta:" Margin="0,0,5,0"/>
            <telerik:RadWatermarkTextBox Width="200"
                                         WatermarkContent="Ej: 2000010509722834"
                                         Text="{Binding FiltroIdVenta, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <Grid>
            <telerik:RadBusyIndicator x:Name="BusyIndicator"
                                      IsBusy="{Binding IsBusy}"
                                      BusyContent="Cargando ventas...">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="100"/>
                    </Grid.RowDefinitions>
                    <telerik:RadGridView x:Name="VentasGrid"
                                         AutoGenerateColumns="False"
                                         IsReadOnly="True"
                                         ItemsSource="{Binding VentasFiltradas}"
                                         Grid.Row="0">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="ID Venta" DataMemberBinding="{Binding OrderId}" Width="200" />
                            <telerik:GridViewDataColumn Header="Fecha" DataMemberBinding="{Binding DateCreated, StringFormat=dd/MM/yyyy}" Width="150" />
                            <telerik:GridViewDataColumn Header="Total Venta" DataMemberBinding="{Binding Payments[0].TotalPaidAmount}" Width="150" />
                            <telerik:GridViewDataColumn Header="Costo Envío" DataMemberBinding="{Binding Payments[0].ShippingCost}" Width="150" />
                            <telerik:GridViewDataColumn Header="Comisión" DataMemberBinding="{Binding SaleFee}" Width="150" />
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" Grid.Row="1">
                        <Button Content="← Anterior" Command="{Binding AnteriorPaginaCommand}" Margin="5" />
                        <TextBlock Text="Página: " VerticalAlignment="Center" Margin="5,0"/>
                        <TextBlock Text="{Binding PaginaActual}" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Text=" de " VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalPaginas}" VerticalAlignment="Center" FontWeight="Bold"/>
                        <Button Content="Siguiente →" Command="{Binding SiguientePaginaCommand}" Margin="5" />
                    </StackPanel>
                </Grid>
            </telerik:RadBusyIndicator>
        </Grid>
    </DockPanel>
</UserControl>
